# Zerino unified requirements

# Capture
ffmpeg-python==0.2.0
watchdog==6.0.0
pynput>=1.7.6           # global hotkey listener (uses macOS Accessibility, no sudo)

# Processing
faster-whisper>=1.0.0   # captions for horizontal video processor (model: small)
Pillow>=10.0.0          # image processor (thumbnails, carousels)

# Publishing
zernio-sdk==1.3.68
python-dotenv==1.2.2
requests>=2.31.0
